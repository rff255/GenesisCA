{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main GenesisCA window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
